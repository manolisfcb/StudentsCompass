.env
.venv/
.venv
.DS_Store
test.db

__pycache__/
*.pyc
*.pyo
*.pyd

# Credentials
# Anclado a la nueva raíz del backend (TASK-037): el patrón lleva barra
# interna, así que git lo resuelve desde este fichero y `app/credentials/` a
# secas dejaría de cubrir el árbol movido.
backend/app/credentials/
*.pickle
google_drive.json
google_oauth_client.json
token.pickle

# Test coverage
htmlcov/
.coverage
coverage.xml
.pytest_cache/
node_modules/

scripts/
scripts/*

# Frontend build output (TASK-038). `node_modules/` above already covers
# frontend/node_modules; the bundle is produced by `npm run build` and by the
# frontend image, never committed.
frontend/dist/
frontend/coverage/

deliverables/

# Baseline visual de TASK-035: 44 PNG, ~18 MB por captura. Se regeneran con
# `cd backend && python scripts/capture_baseline.py` y se publican como
# artefacto de CI; lo versionado es el manifiesto, el OpenAPI y las fixtures.
# Ver docs/refactor/baseline/README.md.
docs/refactor/baseline/screens/


data/
data/*

# TASK-055: names a project and its identities; not this repository's business.
infra/gcp/config.env
